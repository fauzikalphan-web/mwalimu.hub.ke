import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import'./style.css';
const API=import.meta.env.VITE_API_URL||'http://localhost:4000/api/v1';
function App(){const [health,setHealth]=useState<any>(null);const [stats,setStats]=useState<any>(null);useEffect(()=>{fetch(`${API.replace('/api/v1','')}/health`).then(r=>r.json()).then(setHealth).catch(()=>setHealth({ok:false}));},[]);return <main><header><div><h1>Mwalimu Hub KE</h1><span>Learn • Practice • Excel</span></div><b className={health?.ok?'online':'offline'}>{health?.ok?'API ONLINE':'API OFFLINE'}</b></header><section className="grid">{[['Users',stats?.users??'—'],['Published Lessons',stats?.lessons??'—'],['Quizzes',stats?.quizzes??'—'],['Quiz Attempts',stats?.attempts??'—'],['Premium',stats?.premium??'—'],['Successful Payments',stats?.successfulPayments??'—']].map(([a,b])=><article key={a as string}><small>{a}</small><strong>{b}</strong></article>)}</section><nav>Dashboard · Users · Schools · Curriculum · Content · Assessments · Premium · Payments · Analytics · Security</nav><section className="panel"><h2>Platform control center</h2><p>Connect an ADMIN token to load protected analytics and manage published content.</p></section></main>}createRoot(document.getElementById('root')!).render(<App/>);
